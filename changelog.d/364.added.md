Add the SDL `source.build` container image build-provenance surface, expressing base image and digest, image layer chain, structured Dockerfile instructions, classified build arguments, copied source mappings, image-default configuration, source-input checksums, and attestation/verification status, with TechVault webapp parity example coverage (ADR-023).
