Add the SDL `runtime.local_identity` surface, expressing the observed local identity database — `/etc/passwd` users (UID, primary GID/group, GECOS, home, shell, supplemental groups, and distinct disabled/locked/no-login status), `/etc/group` records, and structured sudo/sudoers grants — with provenance and stability classification, and TechVault webapp parity example coverage (ADR-024).
